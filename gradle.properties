# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Android Studio Gradle JDK Configuration:
# If you run on modern system JDKs (like Java 25+), older Gradle Kotlin plugins will crash.
# Please configure Android Studio to use the bundled JDK:
# File -> Settings -> Build, Execution, Deployment -> Build Tools -> Gradle -> Gradle JDK -> Select "Android Studio java home" (JBR)
# For command line builds, run the 'setup_env.bat' script at the project root first.
